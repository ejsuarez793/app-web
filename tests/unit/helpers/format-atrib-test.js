
import { formatAtrib } from 'web/helpers/format-atrib';
import { module, test } from 'qunit';

module('Unit | Helper | format atrib');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatAtrib([42]);
  assert.ok(result);
});

